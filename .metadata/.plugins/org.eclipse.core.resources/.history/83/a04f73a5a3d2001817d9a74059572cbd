package com.internousdev.ecsite02.dao;

import java.sql.Connection;
import java.sql.SQLException;
import java.util.ArrayList;

import com.internousdev.ecsite02.dto.MyPageDTO;
import com.internousdev.ecsite02.util.DBConnector;

public class MyPageDAO {
	private DBConnector db=new DBConnector();
	private Connection con=db.getConnection();

	public ArrayList<MyPageDTO> getMyPageUserInfo(String item_transaction,String user_master_id) throws SQLException{
		ArryaList<MyPageDTO> myPageDTO=new ArrayList<>();
		String sql="SELECT ubit.id, iit.item, ubit.total_price, ubit_total_count, ubit.pay, ubit.insert_date "
				 + "FROM user_buy_item_transaction ubit "
				 + "LEFT JOIN item_info_transaction iit "
				 + "ON ubit.item_transaction_id=iit.id "
				 + "WHERE ubit.item_transaction_id=? AND ubit.user_master_id=? "
				 + "ORDER BY insert_date DESC";

	}


}
